{
    "courses": {
    {% for course in courses %}
        "{{course.course_code}}": {
            "course_name":"{{course.course_name}}",
            "url":"{{course.course_url}}"
        },
    {% endfor %}
    }
}